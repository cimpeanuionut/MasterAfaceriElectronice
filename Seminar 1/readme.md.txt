Seminarul 1.

Vom face structura bazei de date si vom face o noua aplicatie folosind ASP.Net Core MVC. Vom utiliza .NET5.
Vom folosi EntityFramework pentru a face entitati peste tabela pentru a putea face operatiile CRUD de baza.

In acest seminar vom invata sa facem CRUD peste o tabela simpla, vom construi o interfata folosind boostrap si jquery, vom implementa Repository ca design patten.